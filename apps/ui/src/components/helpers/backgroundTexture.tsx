import { Box } from '@chakra-ui/react';

export const BackgroundTexture = () => {
  return <Box className="texture" position="absolute" bottom={0} left={0} maxH={1000}> <span className="main-gradient"></span> </Box>
}
