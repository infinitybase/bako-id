query records($owner: String) {
    Records(
        where: { owner: { _eq: $owner } }
    ) {
        id
        name
        owner
        resolver
        name_hash
        timestamp
        period
    }
}