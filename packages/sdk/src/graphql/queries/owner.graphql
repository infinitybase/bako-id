query owner($name: String, $network: network) {
    Records(
        limit: 1
        where: {
            name: { _eq: $name },
            network: { _eq: $network }
        }
    ) {
        id
        name
        owner
        resolver
        name_hash
        timestamp
        period
    }
}