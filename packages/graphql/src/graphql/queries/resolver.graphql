query resolver($name: String, $network: network) {
    AddressResolver(
        limit: 1
        where: {
            name: { _eq: $name },
            record: {
                network: { _eq: $network }
            }
        }
    ) {
        resolver
    }
}